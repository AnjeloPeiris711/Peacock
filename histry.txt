ssh:{
                    description:'SSH conection',
                    usage: 'ssh',
                    fn: async () => {
                        const data = await getssh()
                        if(typeof data.members==='undefined') {
                            terminal.current.pushToStdout("eroor api server")
                        }
                        else{
                            data.members.map((member,i)=>(
                                terminal.current.pushToStdout(<p key={i}>{member}</p>)
                            ))
                        }
                        const postdata = await postssh()
                        //postdata.value ="heee its work"
                        if(postdata.ok){
                            console.log('no dout')
                        }
                    }
                },